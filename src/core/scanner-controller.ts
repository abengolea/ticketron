// This file is intentionally left blank.
// The logic has been moved directly into the components that use it
// (e.g., validator-debug/page.tsx) to ensure it is only ever executed on the
// client-side, preventing server-rendering errors.
